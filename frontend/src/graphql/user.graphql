query queryUser($uid: String!) {
    queryUser(uid: $uid) {
        uid,
        displayName,
        email,
        photoURL,
        nickname,
        locale
    }
}


mutation updateUserData($userInput: UserInput!) {
    updateUserData(user: $userInput) {
        uid,
        displayName,
        email,
        photoURL,
        nickname
    }
}


