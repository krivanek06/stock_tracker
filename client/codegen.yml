
schema:
  - http://localhost:4000/graphql/

documents:
  - src/**/*.graphql

generates:
  ./src/app/core/api/customGraphql.service.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-apollo-angular
